// ltl-hmac/main.ltl
//
// HMAC-SHA256 / SHA3 with test-vector coverage.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-hmac v0.1.0")
    io::println("HMAC-SHA256 / SHA3 with test-vector coverage.")
    0
}
