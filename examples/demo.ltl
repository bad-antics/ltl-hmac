// examples/demo.ltl — runnable demo for ltl_hmac

import std::io
use ltl_hmac as lib

fn main() {
    io::println("ltl_hmac demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}